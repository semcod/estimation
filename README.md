# estimation

Resource estimation for ticket-bound URI processes.

This repository is governed by `wellmanifest/new-project` and adopts the
`wellmanifest/logs` event contract. Runtime ownership remains in `semcod`.
